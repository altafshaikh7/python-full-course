a =(1,2,3,5,6)  
print(type(a)) # this will show the type of variable a which is tuple

b=(1)
print(type(b)) # this will show the type of variable b which is int

c=(1,)
print(type(c)) # this will show the type of variable c which is tuple

#tuple can not be changed once it is created. it is immutable
# a[0]= 10 # this will give an error because we can not change the value of tuple